version: "3.8"
services:
  minidns:
    image: minidns:1.0.0
    container_name: minidns
    environment:
      DOMAIN: local
      PRIMARY_DNS: 9.9.9.9
      SECONDARY_DNS: 1.1.1.1
    volumes:
      - /etc/hosts:/data/hosts:ro
    ports:
      - "53:53/udp"
      - "53:53/tcp"
